# 'PourPackage' $Package $Target $Height
#Prompt 'PourPackage num params = ' + param[0]
#Wait 1

If
	$param[0] > 0
Then
	$Package = param[1]
	$Target = param[2]
	$Height = param[3]
Endif

#Move 15cm over container over 1/2 second
$Package moveAbove $Target $Height 0.5
Wait .6

#Pour
$Package localrotateX 130 1
wait 1

